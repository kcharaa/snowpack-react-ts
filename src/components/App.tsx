import React from 'react';
import './App.css';

interface AppProps {}

function App({}: AppProps) {
    return (
        <div className="App">
            APP
        </div>
    );
}

export default App;